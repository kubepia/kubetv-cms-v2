apiVersion: v1
kind: Service
metadata:
  name: cms-v1
spec:
  type: ClusterIP
  selector:
    app: cms-v1
  ports:
    - name: nodejs
      port: 3002
      targetPort: nodejs
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: sam-endpoint
  labels:
    app: endpoint
data:
  config.js: |-
    module.exports = {
        endpoint : {
            account: "http://localhost:8080/",
            cms: "http://sam-cranberry-cms:8080/"
        }
    }

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cms-v1
  labels:
    app: cms-v1
    version: v1
spec:
  selector:
    matchLabels:
      app: cms-v1
      version: v1
  replicas: 1
  template:
    metadata:
      name: cms-v1
      labels:
        app: cms-v1
        version: v1
    spec:
      containers:
        - name: cms-v1
          image: g3.skb:8500/btv/cms-v1
          imagePullPolicy: Always
          volumeMounts:
          - name: app-config
            mountPath: /app/config.js
            subPath: "config.js"
          ports:
          - name: nodejs
            containerPort: 3000
          resources:
            requests:
              cpu: 300m
              memory: 500Mi
            limits:
              cpu: 1000m
              memory: 2048Mi
      volumes:
      - name: app-config
        configMap:
          name: sam-endpoint
          items:
          - key: config.js
            path: config.js
            